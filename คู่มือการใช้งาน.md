# คู่มือการใช้งาน CCTV Automation

ระบบแคปภาพกล้องวงจรปิดภายในเครื่อง พร้อมหน้าเว็บสำหรับบันทึกภาพสด บันทึกข้ามคืน และดึงภาพย้อนหลังจาก NVR

เลือกใช้งานได้ 2 แบบ: **Web base** เปิดผ่าน `http://127.0.0.1:8787/` หรือ **Desktop app (Electron)** ที่เปิด Python backend ให้เอง

## สิ่งที่โปรเจกต์ทำได้

- แคปภาพรวมจากกล้องที่ตั้งไว้ทั้งหมด 18 ช่อง
- สร้างภาพรวมอัตโนมัติ และเก็บภาพกล้องแยกแต่ละช่อง
- เก็บภาพกล้องทุกช่องและภาพรวมไว้ในโฟลเดอร์ `รวมกล้อง` เดียวกัน เพื่อลดไฟล์ซ้ำ
- บันทึกข้ามคืนตามเวลาที่กำหนด เช่น `23:00` และ `04:00`
- บันทึกย้อนหลังแบบข้ามคืน โดยใช้วันที่เริ่มต้นและวันที่ถัดไป
- แสดงผลภาพในเว็บ เปิดดู หรือดาวน์โหลดได้
- ดูวิดีโอสดจากกล้องที่เลือกผ่านโหมด **LIVE CCTV**
- รวมภาพของรอบเวรเป็นไฟล์ ZIP ไว้ในโฟลเดอร์ `exports/` ได้
- ยกเลิกงานที่กำลังทำอยู่ได้

## ไฟล์ `.env` ต้องขอจากใคร

ให้ขอไฟล์ `.env` จาก **พี่หมี** เพราะไฟล์นี้มีข้อมูลสำหรับเชื่อมต่อ NVR เช่น Host, Username และ Password

ห้ามสร้างค่าเองจาก `.env.example` หากยังไม่มีข้อมูล NVR และห้ามส่งไฟล์ `.env` เข้า GitHub หรือแชตสาธารณะ

## ต้องวางไฟล์ `.env` ไว้ที่ไหน

วางไฟล์ชื่อ `.env` ไว้ใน **โฟลเดอร์หลักของโปรเจกต์** ระดับเดียวกับโฟลเดอร์ `Source Code`

โครงสร้างที่ถูกต้อง:

```text
CCTV Autometion/
├── คู่มือการใช้งาน.md
├── Source Code/            ← โค้ดทั้งหมด, app.py และไฟล์พัฒนา
├── Picture/                ← ภาพกล้องและไฟล์ส่งงาน
├── .env                    ← วางไฟล์จากพี่หมีตรงนี้
├── ติดตั้ง CCTV Automation macOS.dmg
├── ติดตั้ง CCTV Automation Windows.exe
├── เปิด Web Base macOS.command
├── เปิด Web Base Windows.bat
├── CCTV Automation.app
└── CCTV Automation Windows/
```

ห้ามวาง `.env` ไว้ใน `static/` หรือ `captures/` และตรวจสอบว่าไฟล์ชื่อ `.env` จริง ๆ ไม่ใช่ `.env.txt`

### ตำแหน่งบน macOS เครื่องนี้

```text
/Volumes/DriveG/dev/workspace/github/CCTV Autometion/.env
```

### ตำแหน่งบน Windows

วางไว้ในโฟลเดอร์ที่เก็บโปรเจกต์ เช่น:

```text
C:\CCTV Autometion\.env
```

## โปรแกรมที่ต้องติดตั้ง

จำเป็น:

- Python 3 สำหรับรัน backend
- ffmpeg สำหรับอ่าน RTSP และสร้างภาพรวม

สำหรับ Web base ไม่ต้องติดตั้ง Node.js หลังจาก build หน้าเว็บแล้ว ส่วนการ build Desktop app ต้องใช้ Node.js แต่ผู้ใช้ที่ติดตั้งแอปสำเร็จไม่ต้องเปิด Node เอง

## ติดตั้งบน macOS

วิธีง่ายที่สุด ดับเบิลคลิกไฟล์:

```text
ติดตั้งระบบ.command
```

หรือใช้ Homebrew:

```bash
brew install python ffmpeg
```

จากนั้น:

1. ขอไฟล์ `.env` จากพี่หมี
2. วาง `.env` ไว้ที่โฟลเดอร์หลักของโปรเจกต์
3. ดับเบิลคลิก `เปิด Web Base macOS.command` เพื่อเปิด Web base

ถ้าต้องการรันด้วยคำสั่ง:

```bash
cd "/Volumes/DriveG/dev/workspace/github/CCTV Autometion/Source Code"
python3 app.py
```

## ติดตั้งบน Windows

ถ้าเครื่องมี `winget` ให้ดับเบิลคลิก:

```text
ติดตั้งระบบ_windows.bat
```

หรือเปิด PowerShell/Command Prompt แล้วใช้:

```bat
winget install --id Python.Python.3.12 --exact --source winget
winget install --id Gyan.FFmpeg --exact --source winget
```

หลังติดตั้ง ให้ปิดแล้วเปิดหน้าต่าง Terminal ใหม่ จากนั้น:

1. ขอไฟล์ `.env` จากพี่หมี
2. วาง `.env` ไว้ที่โฟลเดอร์หลักของโปรเจกต์
3. ดับเบิลคลิก `เปิด Web Base Windows.bat`
4. โปรแกรมจะเปิด Web base ผ่านเบราว์เซอร์

## เปิดเป็น Desktop app (Electron)

แอป Desktop แสดงหน้า CCTV เดิมในหน้าต่างของตัวเอง และเปิด Python backend ให้อัตโนมัติ

- ต้องติดตั้ง Python 3 และ FFmpeg ตามหัวข้อด้านบน
- ไฟล์ติดตั้ง macOS/Windows และขั้นตอน build อยู่ใน [Source Code/developer/electron/README.md](Source%20Code/developer/electron/README.md)
- ถ้าเปิด `CCTV Automation.app` หรือ `CCTV Automation Windows` จาก root นี้ ระบบจะใช้ไฟล์ `.env` ที่ root เดียวกันโดยอัตโนมัติ
- ถ้าติดตั้งจาก DMG หรือ Setup.exe ลงเครื่องอื่น ให้ขอไฟล์ `.env` จาก **พี่หมี** แล้ววางในโฟลเดอร์ข้อมูลของแอปตามคู่มือ Electron

## วิธีใช้งานหน้าเว็บ

### ดูวิดีโอสด

1. เปิดแท็บ **LIVE CCTV**
2. เลือกหมายเลขกล้อง
3. กด **เปิดภาพสด**
4. เมื่อต้องการหยุด กด **หยุดภาพสด**

ระบบเปิดภาพสดทีละกล้องผ่าน FFmpeg เพื่อไม่ให้เครื่องทำงานหนักเกินไป

### บันทึกข้ามคืน

1. เลือกแท็บ **บันทึกข้ามคืน**
2. ตั้งเวลาแคป 2 รอบ เช่น `23:00` และ `04:00`
3. กด **บันทึกเวลา**
4. กด **เปิดรันข้ามคืน**
5. เปิดเครื่องและโปรแกรมทิ้งไว้จนกว่างานจะเสร็จ

### บันทึกย้อนหลัง

1. เลือกแท็บ **บันทึกย้อนหลัง**
2. เลือกวันที่เริ่มต้น
3. รอบแรกจะเป็นวันที่เลือก เช่น `23:00`
4. รอบที่สองจะเป็นวันถัดไป เช่น `04:00`
5. กด **บันทึกย้อนหลัง 2 ช่วงเวลา**
6. ดูความคืบหน้าและภาพได้ที่หัวข้อ **ผลลัพธ์ภาพ**

หากงานใช้เวลานานหรือสั่งผิด ให้กด **ยกเลิกงาน**

## ตำแหน่งผลลัพธ์

```text
captures/YYYY-MM-DD/HH-MM/
├── รวมกล้อง/
│   ├── camera-01_....jpg
│   └── ภาพรวม-18-ช่อง_....jpg
└── manifest.json
```

ที่หัวข้อ **ผลลัพธ์ภาพ** ให้กด **รวมชุดส่งงาน** ระบบจะรวมภาพรวม ภาพกล้องเดี่ยว และ `manifest.json` ของรอบเวรไว้เป็นไฟล์ ZIP ในโฟลเดอร์ `exports/` ซึ่งอยู่ในโปรเจกต์เดียวกัน พร้อมเปิดดาวน์โหลดให้

ถ้าบางกล้องไม่มีไฟล์ย้อนหลังหรือ Playback ตอบไม่ทัน ระบบจะแสดงเป็น **เสร็จบางส่วน** พร้อมระบุหมายเลขกล้องที่มีปัญหา ภาพจากกล้องอื่นยังใช้งานได้

## แก้ปัญหาเบื้องต้น

- เชื่อมต่อ NVR ไม่ได้: ตรวจสอบเครือข่ายและข้อมูลใน `.env`
- ไม่พบ ffmpeg: ดับเบิลคลิก `ติดตั้งระบบ_windows.bat` แล้วเปิดโปรแกรมใหม่อีกครั้ง โปรแกรมจะค้นหา ffmpeg ในตำแหน่งติดตั้งมาตรฐานของ Windows ให้อัตโนมัติ
- ได้ภาพไม่ครบ: ดูหมายเลขกล้องที่แจ้งเตือน และตรวจ Playback ของกล้องนั้นที่ NVR
- ดูรายละเอียดเพิ่มเติมได้ที่ `data/app.log`

## ความปลอดภัย

ไฟล์ `.env` มีข้อมูลสำหรับเข้าถึงภาพกล้อง เก็บไว้ในเครื่องที่ได้รับอนุญาตเท่านั้น และไม่ควรแนบไฟล์นี้ไปกับเอกสารหรืออัปโหลดสาธารณะ
